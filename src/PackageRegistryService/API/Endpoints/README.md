# API endpoints

This folder contains route-group composition for versioned registry APIs and schema resources. Endpoint classes bind paths and HTTP verbs to handlers and attach routing or OpenAPI metadata.

Business rules, database queries, and response construction belong in handlers so route registration remains easy to scan.
