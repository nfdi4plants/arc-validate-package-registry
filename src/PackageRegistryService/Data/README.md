# Data initialization

This folder contains startup-time database decisions and initial population of an empty registry from the staged package source. It coordinates deployment state and seeding without defining the persisted entity shapes.

Entity configuration belongs under `Models`, while historical schema changes belong under `Migrations`.
