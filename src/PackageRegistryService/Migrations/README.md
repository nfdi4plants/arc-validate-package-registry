# Database migrations

This folder contains Entity Framework Core's ordered history of registry database schema changes and the generated model snapshot used to calculate future changes.

Migration and designer files are generated or scaffolded artifacts. Add schema changes through the repository's Entity Framework workflow and avoid hand-maintaining documentation or application behavior inside generated snapshots.
