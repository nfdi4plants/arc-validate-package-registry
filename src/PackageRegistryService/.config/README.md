# Local tool configuration

This folder contains project-scoped .NET tool declarations used during development. Keeping the tool manifest beside the service makes tool versions reproducible without installing those tools globally.

Application code and runtime configuration do not belong here.
