# OpenAPI generation

This folder customizes the generated OpenAPI description of the minimal API. Processors define public schema shapes, enrich operations with human-readable metadata and security requirements, and exclude non-API routes.

The code here documents and adapts the registered endpoints; it does not implement endpoint behavior.
