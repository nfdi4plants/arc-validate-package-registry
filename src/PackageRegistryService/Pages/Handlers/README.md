# Page handlers

This folder contains browser-request handlers for the server-rendered website. Handlers load the required state, select a page component, apply the shared layout, and return typed HTML, redirect, or error results.

Reusable rendering belongs in `Components`; reusable application capabilities belong in `Services`.
