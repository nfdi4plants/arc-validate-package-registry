# Website pages

This folder contains the human-facing, server-rendered registry website. Page endpoint registration connects browser routes to handlers, while nested folders separate request orchestration from reusable HTML rendering.

The website consumes the same service models and services as the API but is not part of the programmatic API contract.
