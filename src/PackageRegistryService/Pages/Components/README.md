# Page components

This folder contains focused HTML renderers for shared layout, navigation, documentation, release information, package summaries, and package details. Components transform already-prepared values into markup and should avoid owning database access or route orchestration.

User-provided values must be encoded at the component boundary whenever they are inserted into HTML.
