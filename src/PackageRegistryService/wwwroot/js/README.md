# Browser scripts

This folder contains browser-side JavaScript loaded by the server-rendered registry pages. It is reserved for progressive enhancements and vendored browser libraries; core registry behavior must remain on the server.

Vendored assets should retain a recognizable upstream filename and be updated deliberately.
